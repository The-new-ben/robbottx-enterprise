# Changelog

## Unreleased

- Established the clean RobbottX 360-degree repository and governance boundary.
- Added the candidate mobile-manipulator canonical dataset and deterministic
  WordPress publication snapshot.
- Added the original Precision Atlas block theme and projection-only core
  plugin.
- Added local contract, forbidden-input, theme, PHP, snapshot, and preview
  verification.

## 0.1.4

- Increased muted small-text contrast with a measured accessibility margin.
- Added an original, cache-versioned favicon that matches the RobbottX
  geometric brand mark.
- Added deterministic homepage Open Graph metadata using the compact summary
  card format.
- Removed unused commerce, review, and filter assets from the catalog
  homepage while preserving every commerce and account context.
- Added automated condition-matrix checks for homepage asset discipline.

## 0.1.3

- Added versioned plugin-owned responsive wrapping for long engineering model,
  evidence, and source identifiers in the featured configuration.
- Replaced public project and publication vocabulary with established catalog
  language for specifications, compatibility, documents, and supplier context.
- Added a public-language release gate covering audience, internal vocabulary,
  em dashes, formulaic generated phrasing, and established-platform voice.
- Updated the static theme language and navigation as theme version 0.1.3.
- Removed unsupported field-level search claims and unscoped completeness
  language from the public theme.
- Replaced the directly readable plugin record JSON with an executable,
  direct-request-guarded PHP resource.
- Changed the public health response from internal snapshot and projection
  terms to catalog record language.
- Minted immutable record build
  `RBTX:S:019f8ff9-bcc4-7872-ab79-4a4d594d0aa3`, bound to payload SHA-256
  `8e859aaedfac1e7204399d050ec0da19f71f00113842edd9622c969dad4deb52`.
- Added exact package inventories, public artifact hash and size checks,
  server-side artifact verification, unique deploy routes, ambiguous-create
  recovery, and independent cleanup proofs for plugin and theme deployment.

## 0.1.2

- Added deterministic homepage description and conservative WebSite/WebPage
  JSON-LD, using Rank Math filters when available and a guarded fallback when
  its frontend head is absent.
- Serialized the static block-theme group wrappers so every public template
  exposes exactly one keyboard-targetable `main` landmark.
- Relied on WordPress core for the single skip link and capability-gated theme
  overwrites with `update_themes`.

## 0.1.1

- Added the public integrity healthcheck and an in-body runtime marker used by
  the create-call-verify-delete deployment loop.
- Vendored Plugin Update Checker v5.6 (`load-v5p6`) as the guarded fallback
  update path.
